machine:
  node:
    version: 6.10.3
dependencies:
  pre:
    - npm install -g @angular/cli
test:
  override:
    - "true"
deployment:
  production:
    branch: master
    commands:
      - ng build
      - sh ./deploy.sh