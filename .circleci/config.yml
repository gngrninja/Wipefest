version: 2
jobs:
  build:
    working_directory: ~/wipefest
    docker:
      - image: joshyaxley/angular-cli
    steps:
      - checkout
      - run: npm install
      - run: ng build
      - run: ls dist
      - deploy:
          name: Development
          command: sh deploy.sh